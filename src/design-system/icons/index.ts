// 04 아이콘 (Icons) — Figma 「디자인 시스템」 04 섹션과 1:1.
//
// 규칙 (공통 규칙은 README 「폴더 구성」 표)
// - 아이콘 하나 = SVG 를 감싼 React 컴포넌트 하나 (파일 하나). 이름은 Figma 아이콘 이름을 PascalCase 로: `icon/search` → `IconSearch`
// - 크기는 `size` prop (기본 16), 색은 `currentColor` 를 따르게 해서 부모 글자색을 그대로 받는다.
// - 장식용이면 `aria-hidden`, 의미가 있으면 `title` prop 으로 접근성 이름을 받는다.
// - 아이콘을 만들면 아래에 export 를 한 줄씩 추가한다.
//   export { IconSearch } from './IconSearch'
export {}
