import { Card } from '@/design-system'

/** 인스펙터 화면. 이 폴더(src/features/inspector/) 안에서 만든다 */
export function InspectorPage() {
  return (
    <Card title="준비 중">
      <p className="text-body-13" style={{ margin: 0, color: 'var(--color-text-tertiary)' }}>
        인스펙터 화면은 아직 만들지 않았습니다.
      </p>
    </Card>
  )
}
