import { Card } from '@/design-system'

/** 트랜잭션 화면. 이 폴더(src/features/transactions/) 안에서 만든다 */
export function TransactionsPage() {
  return (
    <Card title="준비 중">
      <p className="text-body-13" style={{ margin: 0, color: 'var(--color-text-tertiary)' }}>
        트랜잭션 화면은 아직 만들지 않았습니다.
      </p>
    </Card>
  )
}
