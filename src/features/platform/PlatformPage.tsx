import { Card } from '@/design-system'

/** 플랫폼 상태 화면. 이 폴더(src/features/platform/) 안에서 만든다 */
export function PlatformPage() {
  return (
    <Card title="준비 중">
      <p className="text-body-13" style={{ margin: 0, color: 'var(--color-text-tertiary)' }}>
        플랫폼 상태 화면은 아직 만들지 않았습니다.
      </p>
    </Card>
  )
}
