import { Card } from '@/design-system'

/** 경보 화면. 이 폴더(src/features/alerts/) 안에서 만든다 */
export function AlertsPage() {
  return (
    <Card title="준비 중">
      <p className="text-body-13" style={{ margin: 0, color: 'var(--color-text-tertiary)' }}>
        경보 화면은 아직 만들지 않았습니다.
      </p>
    </Card>
  )
}
