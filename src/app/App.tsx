// 앱 뼈대. 라우터 · TanStack Query 연결은 쓰는 화면이 생길 때 여기에 붙인다.
// 지금은 디자인 시스템 미리보기만 보여준다.
import { DesignSystemPreview } from './DesignSystemPreview'

export function App() {
  return <DesignSystemPreview />
}
